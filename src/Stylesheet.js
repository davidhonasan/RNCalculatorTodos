import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  container: {
    padding: 25,
  },
  title: {
    fontWeight: 'bold',
    fontSize: 36,
    textAlign: 'center',
  },
});

export default Styles;
